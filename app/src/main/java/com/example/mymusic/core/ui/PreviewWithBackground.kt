package com.example.mymusic.core.ui

import androidx.compose.ui.tooling.preview.Preview

private const val backgroundColorCode = 0xFF837469
/**
 * Preview with a background color. Add this annotation
 * to a composable to render with a background
 */
@Preview(showBackground = true, backgroundColor = backgroundColorCode)
annotation class PreviewWithBackground
